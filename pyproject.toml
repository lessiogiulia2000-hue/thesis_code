[project]
name = "thesis-code"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "networkx",
    "POT",
    "pandas",
    "matplotlib",
    "scipy",
    "scikit-learn",
    "scikit-bio",
    "seaborn",
    "tqdm",
    "EDRep",
]
